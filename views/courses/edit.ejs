<!DOCTYPE html>
<html>
<head>
  <title>Uredi tečaj</title>
</head>
<body>
  <h1>Uredi tečaj</h1>
  <form action="/courses/edit/<%= course.id %>" method="POST">
    <input type="text" name="name" value="<%= course.name %>" required><br>
    <input type="number" name="duration" value="<%= course.duration %>" required><br>
    <input type="text" name="level" value="<%= course.level %>" required><br>
    <input type="number" name="price" value="<%= course.price %>" required><br>
    <button type="submit">Spremi</button>
  </form>
</body>
</html>
